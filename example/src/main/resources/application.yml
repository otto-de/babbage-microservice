info:
  app:
    name: babbage-example


management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: loggers, info, health
    loggers:
      enabled: true
spring:
  webflux:
    base-path: /foo
